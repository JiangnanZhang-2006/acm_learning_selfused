## Learned User Preferences

- 做算法/竞赛题时，若用户明确要求「不要联网搜索」或「你觉得的标准做法」，则按题意推导，不进行联网搜索

## Learned Workspace Facts

- Codeforces 题解代码位于 `codeforces/` 目录
- 本地 macOS 上若 `bits/stdc++.h` 编译失败，可改用标准头文件（如 `iostream`、`algorithm`、`list` 等）
