# 位运算

## 基本运算符

- `&` (按位与)：两个位都为1时结果为1
- `|` (按位或)：两个位至少一个为1时结果为1
- `^` (按位异或)：两个位不同时结果为1
- `~` (按位取反)：0变1，1变0
- `<<` (左移)：左移n位相当于乘以2^n
- `>>` (右移)：右移n位相当于除以2^n

## 常用技巧

- 判断奇偶：`n & 1`
- 取最低位1：`n & -n`
- 清除最低位1：`n & (n - 1)`
- 判断2的幂：`n > 0 && (n & (n - 1)) == 0`
